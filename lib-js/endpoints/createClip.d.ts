import { createClipResponse } from "../types/endpoints/createClip";
export declare function createClip(sym: string, broadcasterID?: string, hasDelay?: boolean, customToken?: string): Promise<createClipResponse>;

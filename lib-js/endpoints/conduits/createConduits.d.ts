import { getConduitsResponse } from "../../types/endpoints/conduits/getConduits";
export declare function createConduits(sym: string, shardCount: number, customToken?: string): Promise<getConduitsResponse>;
